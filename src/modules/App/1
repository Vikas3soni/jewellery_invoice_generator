.root {
    padding: 90px;
    display: flex;
    flex-direction: row;
}
  
@media (min-width: 768px) { /* Adjust styles for larger screens (laptops) */
.root {
    flex-direction: row; /* Allow side-by-side layout */
    justify-content: space-between; /* Space evenly between mobile app login and web form */
}

.login-form { 
    width: 300px; /* Maintain a fixed width */
    
}
}

  /* Other responsive styles for various elements (heading, button, etc.) */
  
.heading {
margin-bottom: 20px;
color: #333;
}

.button {
    padding: 10px 20px;
    background-color: #007bff;
    color: #fff;
    border: none;
    border-radius: 3px;
    cursor: pointer;
    transition: background-color 0.3s;
    margin: 0 auto;
}

.login-form-centered {
display: flex;
flex-direction: column;
align-items: center;
width: 300px;
justify-content: center; /* Center vertically and horizontally */
margin: 0 auto; /* Add margin for better centering on some layouts */
box-shadow: '0 2px 5px rgba(0, 0, 0, 0.1)';
}
